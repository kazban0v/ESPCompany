# Инструкция по деплою Django проекта на Railway

## Шаг 1: Подготовка проекта

✅ Все файлы уже подготовлены:
- `backend/Procfile` - команда запуска
- `backend/runtime.txt` - версия Python
- `backend/requirements.txt` - зависимости (обновлены)
- `backend/esp_site/settings.py` - настройки для production
- `.gitignore` - исключения для Git

## Шаг 2: Создание Git репозитория

```bash
# 1. Инициализируйте Git (если еще не сделано)
cd C:\Users\User\Desktop\ESPCompany
git init

# 2. Добавьте все файлы
git add .

# 3. Создайте первый коммит
git commit -m "Initial commit for Railway deployment"
```

## Шаг 3: Создайте репозиторий на GitHub/GitLab

1. Зайдите на https://github.com (или https://gitlab.com)
2. Создайте новый репозиторий
3. НЕ инициализируйте его с README
4. Скопируйте URL репозитория (например: `https://github.com/username/yourrepo.git`)

## Шаг 4: Загрузите код на GitHub

```bash
# Добавьте удаленный репозиторий
git remote add origin https://github.com/username/yourrepo.git

# Загрузите код
git branch -M main
git push -u origin main
```

## Шаг 5: Создание проекта на Railway

1. Зайдите на https://railway.app
2. Войдите через GitHub (если нужно)
3. Нажмите "New Project"
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий

## Шаг 6: Настройка проекта на Railway

### 6.1. Настройка переменных окружения

В Railway откройте ваш проект → Variables и добавьте:

```
SECRET_KEY=your-secret-key-here (используйте generate_secret_key.py)
DEBUG=False
ALLOWED_HOSTS=your-railway-domain.up.railway.app
DATABASE_URL= (будет добавлен автоматически после создания PostgreSQL)
```

**Важно:** 
- Создайте SECRET_KEY через: `python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"`
- DATABASE_URL добавится автоматически после создания базы данных

### 6.2. Создание PostgreSQL базы данных

1. В Railway проекте нажмите "+ New"
2. Выберите "Database" → "Add PostgreSQL"
3. Railway автоматически добавит переменную `DATABASE_URL`

### 6.3. Настройка Root Directory

В настройках проекта (Settings) найдите "Root Directory" и укажите:
```
backend
```

Это нужно, так как ваш проект находится в папке `backend/`.

### 6.4. Настройка команды запуска

Railway должен автоматически обнаружить `Procfile`, но проверьте в Settings:
- Build Command: (оставьте пустым или `pip install -r requirements.txt`)
- Start Command: `gunicorn esp_site.wsgi --bind 0.0.0.0:$PORT`

## Шаг 7: Деплой и миграции

### 7.1. После первого деплоя выполните миграции

В Railway:
1. Откройте ваш проект
2. Перейдите во вкладку "Deployments"
3. Откройте последний deployment
4. Перейдите в "View Logs"
5. Или используйте Railway CLI:

```bash
# Установите Railway CLI (если нужно)
npm i -g @railway/cli

# Войдите в Railway
railway login

# Подключитесь к проекту
railway link

# Выполните миграции
railway run python manage.py migrate

# Создайте суперпользователя
railway run python manage.py createsuperuser

# Соберите статические файлы
railway run python manage.py collectstatic --noinput
```

## Шаг 8: Настройка кастомного домена (опционально)

1. В Railway проекте перейдите в Settings → Domains
2. Добавьте ваш домен `group.ck-mitra.kz`
3. Настройте DNS записи по инструкции Railway
4. Обновите `ALLOWED_HOSTS` в переменных окружения

## Шаг 9: Обновление кода

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Railway автоматически задеплоит новую версию!

## Проверка работы

1. Откройте ваш Railway домен (например: `your-app.up.railway.app`)
2. Должна открыться главная страница Django
3. Админ-панель: `your-app.up.railway.app/admin/`

## Решение проблем

### Ошибка "ModuleNotFoundError"
- Проверьте, что все зависимости в `requirements.txt`
- Проверьте, что `Root Directory` настроен на `backend`

### Ошибка базы данных
- Убедитесь, что PostgreSQL создан
- Проверьте переменную `DATABASE_URL`

### Статические файлы не работают
- Выполните `collectstatic` через Railway CLI
- Проверьте настройки WhiteNoise в `settings.py`

### Ошибка "ALLOWED_HOSTS"
- Добавьте ваш Railway домен в переменную `ALLOWED_HOSTS`

## Полезные команды Railway CLI

```bash
# Посмотреть логи
railway logs

# Выполнить команду в окружении Railway
railway run python manage.py migrate

# Открыть shell
railway shell

# Просмотреть переменные окружения
railway variables
```

## Готово! 🚀

Ваш Django проект теперь работает на Railway!

